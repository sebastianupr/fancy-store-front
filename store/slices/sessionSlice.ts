import { createSlice } from "@reduxjs/toolkit";

const sessionSlice = createSlice({
  name: 'session',
  initialState: {
    withSession: false,
  },
  reducers: {
  },
})

export default sessionSlice